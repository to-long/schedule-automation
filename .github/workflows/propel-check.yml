name: Scheduled Job

on:
  schedule:
    # Check-in: 8:45 AM GMT+7 (1:45 AM UTC), Mon-Fri only
    - cron: '45 1 * * 1-5'
    # Check-out: 6:00 PM GMT+7 (11:00 AM UTC), Mon-Fri only
    - cron: '0 11 * * 1-5'
  
  # Allow manual trigger for testing
  workflow_dispatch:

jobs:
  attendance:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Run Propel check
        run: npm run propel-check
